# Supabase（Settings → API）
# 本地开发写入 .env.local；部署到 Vercel 时在 Vercel 项目 → Settings → Environment Variables 中配置相同变量
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# 管理员审核(/admin/verify)、模拟用户入驻等需使用。请从 Supabase Dashboard → Settings → API 复制 service_role（勿泄露）
# Vercel 部署时同样在 Environment Variables 中配置 SUPABASE_SERVICE_ROLE_KEY
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# 管理员后台(/admin、/admin/verify) 专用登录，仅此账号可进入
# 部署到 Vercel 时务必在 Vercel 的 Environment Variables 里添加以下三项，否则线上无法登录管理员
ADMIN_PHONE=19955918866
ADMIN_PASSWORD=zyq112816
# 用于签发管理员登录态 Cookie，可随机生成一长串字符串（勿泄露）
ADMIN_SESSION_SECRET=your-random-secret-here
